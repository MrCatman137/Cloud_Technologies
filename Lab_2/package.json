{
  "name": "debounce-translation-lab",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server/server.ts",
    "build": "tsc -p tsconfig.json && node -e \"const fs=require('fs'); const path=require('path'); const src=path.join(process.cwd(),'dist/public/app.js'); const dst=path.join(process.cwd(),'public/app.js'); if (fs.existsSync(src)) fs.copyFileSync(src, dst);\"",
    "start": "node dist/src/server/server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
